# EAS/CI parity: match the local global npm setting so a clean install on EAS
# resolves the RN / Expo peer-dependency graph the same way it does locally.
# Without this, EAS runs strict peer-dep resolution and the "Install
# dependencies" build phase fails.
legacy-peer-deps=true
